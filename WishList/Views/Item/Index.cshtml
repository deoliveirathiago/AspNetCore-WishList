@model List<WishList.Models.Item>

<h1>Wishlist</h1>

<a asp-action="create">Add item</a>

<ul>
    @foreach (var item in Model)
    {
        <li>@item.Description <a asp-action="update" asp-route-id="@item.Id">update</a> <a asp-action="delete" asp-route-id="@item.Id">delete</a></li>
    }
</ul>